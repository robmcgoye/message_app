<% flash.each do |message_type, message| %>
  <div class="ui <%= message_type %> message transmission">
    <i class="close icon"></i>
    <div class="header">
      <%= message %>
    </div>
  </div>
<% end %>